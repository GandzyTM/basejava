читаем количество элементов в коллекции организаций
для элемента коллекции:
    читаем из потока name
    читаем из потока url
    получаем количество элементов в коллекции позиций
    создаем список элементов позиций равным количеству переданных элементов из потока
    для элемента списка позиций:
        записываем в переменные значения из потока
        добавляем полученные переменные в список позиций
    добавляем в резюме:
        тип секции, секцию

